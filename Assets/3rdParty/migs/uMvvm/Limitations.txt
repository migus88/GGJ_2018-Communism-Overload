1. All 'View's should contain the word 'View' at the end. Example: 'TestView'.
2. All 'ViewModel's shoul contain the word 'ViewModel' at the end. Example: 'TestViewModel'.
3. All 'ViewModel's should be global and not inside a namespace. Note: This is temporary behaviour and should be fixed later.